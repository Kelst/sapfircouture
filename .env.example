# ===========================================
# Sapfir Couture - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values

# Domain (required for production)
# Your domain name without https:// prefix
DOMAIN=example.com

# ===========================================
# Application
# ===========================================
# Public URL of your application
NEXT_PUBLIC_APP_URL=https://example.com

# ===========================================
# Database
# ===========================================
# Password for PostgreSQL (change this!)
DB_PASSWORD=your-secure-database-password

# Full database URL (auto-constructed in docker-compose)
# For local development: postgresql://wedding:wedding_secret@localhost:5432/wedding_salon
DATABASE_URL=postgresql://wedding:${DB_PASSWORD}@postgres:5432/wedding_salon

# ===========================================
# Authentication
# ===========================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=generate-a-secure-32-char-secret-key

# ===========================================
# MinIO / S3 Storage
# ===========================================
# MinIO credentials (change these!)
MINIO_USER=minioadmin
MINIO_PASSWORD=your-secure-minio-password

# S3 configuration
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=${MINIO_USER}
S3_SECRET_KEY=${MINIO_PASSWORD}
S3_BUCKET=wedding-uploads

# Public URL for images (through Caddy CDN proxy)
S3_PUBLIC_URL=https://cdn.example.com/wedding-uploads

# ===========================================
# Telegram Notifications (optional)
# ===========================================
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=
# Get by adding @userinfobot to your chat
TELEGRAM_CHAT_ID=

# ===========================================
# Cron Jobs (optional)
# ===========================================
# Secret for cron endpoint authentication
CRON_SECRET=your-cron-secret

# ===========================================
# Cache Configuration (optional)
# ===========================================
# Revalidation times in seconds (set to 0 to disable caching)
# CACHE_STATIC=1800    # Hero slides, collections, styles (default: 30 min)
# CACHE_DYNAMIC=30     # Dresses list (default: 30 sec)
# CACHE_SETTINGS=150   # Settings, social links (default: 2.5 min)
